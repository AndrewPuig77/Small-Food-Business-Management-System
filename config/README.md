# Configuration

This folder will contain configuration files.

## Files (to be created)

- `database.config.js` - Database connection settings
- `app.config.js` - Application settings
- `.env.example` - Environment variables template

## Note

Actual `.env` file with secrets will NOT be committed to git (already in .gitignore).
